# 功能追踪与开发进度 (Feature Tracker)
> 最后更新时间: 2026-05-22

## 🏆 已完成功能 (Completed)

### 1. 开发环境与基础架构
- [x] **项目初始化**: 技术栈确立 (Electron + React + TypeScript + Vite)。
- [x] **状态管理**: 建立了持久化的 ProjectStore 和运行时的 EditorStore (Zustand + Immer)。
- [x] **测试环境**: 集成 Jest (单元测试) 和 Playwright (E2E)。
- [x] **镜像加速**: 配置了 npmmirror 和 Electron 国内下载镜像。

### 2. 文件管理器 (File Manager - Phase 1)
- [x] **项目加载**: 支持通过原生对话框打开本地项目文件夹。
- [x] **启动恢复**: 应用重新启动时自动恢复上次打开的项目，并优先恢复上次选中的文件。
- [x] **文件扫描**: 自动递归/平铺扫描目录下的所有 `.csv` 文件。
- [x] **智能解析**:
  - 自动检测编码 (UTF-8, GBK 等)。
  - 遵循 RFC 4180 标准解析 CSV。
- [x] **列表展示**: 侧边栏展示文件名、选中状态、未保存标记 (*)。

### 3. 表格核心编辑器 (Editor Core - Phase 2)
- [x] **海量数据渲染**: 主编辑表格使用 Glide Data Grid，支持 50,000+ 行流畅滚动。
- [x] **多列支持**: 自动解析表头，动态渲染多列网格。
- [x] **滚动同步**: 实现了表头与数据内容的横向滚动精确同步。
- [x] **单元格交互**:
  - 点击选中单元格并高亮。
  - Key 值 (第一列) 自动校验 (空值、非法字符红框预警)。
- [x] **富文本编辑器**:
  - 集成 CodeMirror 6。
  - **TMP 标签 Typora 式渲染**: 光标在内显示源码+语法高亮，光标离开隐藏标签并渲染样式效果。
  - 支持 27+ 种 TMP 标签 (`<b>`, `<i>`, `<color>`, `<size>`, `<u>` 等)。
  - 双向同步: 编辑文本实时同步到表格数据。
  - 自动换行: 支持长文本自动折行。
  - **可调整高度**: 拖动分隔条调整表格与编辑栏的高度比例。
  - **交互增强 (New)**:
    - **工具栏**: 提供 B/I/U/S, 颜色(原生取色器), 字号(模态框), 对齐, 缩进等快捷操作。
    - **右键菜单**: 上下文相关的格式化选项与快捷设置。
    - **快捷键**: 常用格式的键盘支持 (Ctrl+B/I/U)。

### 4. 功能面板 (Function Panel - Phase 3)
- [x] **布局架构**: 实现了选项卡切换。
- [x] **查找与替换**:
  - 支持字符串/正则表达式。
  - **模糊搜索**: 普通模式下支持空格分词多关键字匹配 (类似 Unity)。
  - 支持单文件/全项目搜索。
  - **自动定位**: 点击结果自动跳转、滚动并高亮单元格。
  - **批量替换**: 跨文件执行“全部替换”。
- [x] **全局校验**: Key 重复性检测、未翻译项快速定位。

### 5. 数据持久化与校验
- [x] **自动校验**: 后台实时检测 Key 重复 (Real-time Indexing)。
- [x] **文件保存**: 将修改写回 `.csv` 文件 (Manual + Auto-save)。

### 6. 高级表格操作 (Advanced Table Operations - Phase 4)
- [x] **行号栏 (Row Headers)**: 左侧固定行号，支持整行选中、Shift多选。
- [x] **上下文菜单**: 右键菜单支持 批量 插入/删除行、列，支持指定数量插入。
- [x] **数据操作**: 实现多行/多列的 Store Actions (batch operations)。
- [x] **剪贴板增强**: 实现多单元格/整行的复制粘贴 (Ctrl+C/V)，兼容 Excel/TSV。

### 7. 交互增强 (Interaction - Phase 5)
- [x] **智能填充 (Fill Handle)**: 使用 Glide 原生填充柄；普通内容按原生复制填充，数字/带数字后缀内容支持递增填充。
- [x] **表头编辑**: 通过 Glide 表头菜单重命名列。
- [x] **矩形选区**: Shift+Click 支持矩形区域选中。

## 📅 下一步计划 (Upcoming)

### 性能与其他
- [x] **性能优化**: 支持 50,000+ 行流畅滚动。 (100k+ 优化暂不需)
- [ ] **搜索增强**: 正则表达式高亮预览。

### 表格增强
- [x] **Toggle 列编辑**: 布尔列 (0/1, true/false) 显示复选框 UI，点击快速切换值。
  - 点击命中范围限制在单元格中央开关控件，点击周围空白不会切换布尔值
  - 持久化 Toggle 列配置
- [x] **列宽调整**:
  - 拖拽表头调整列宽
  - 双击自适应内容宽度
  - 列宽设置持久化

### 8. 撤销/重做 (Undo/Redo System)
- [x] **全局历史栈**: 统一管理所有操作历史，支持 100 步撤销。
- [x] **全功能支持**:
  - **单元格编辑**: 撤销内容修改。
  - **批量操作**: 撤销粘贴 (Paste)、填充 (Fill)、全部替换 (Replace All)。
  - **结构变更**: 撤销行/列的插入、删除、复制 (Duplicate Rows)。
- [x] **快捷键集成**: `Ctrl+Z` (Undo), `Ctrl+Shift+Z` / `Ctrl+Y` (Redo)。
- [x] **交互优化**:
  - **自动聚焦 (Auto Focus)**: 撤销/重做后自动滚动并选中受影响区域。
  - **内联撤销 (Inline Undo)**: 编辑模式下通过 `Ctrl+Z` 无缝触发全局撤销。

### 9. 文件分组与配置 (File Grouping & Config)
- [x] **分组管理**: 支持创建、删除、重命名分组，通过右键菜单移动文件。
- [x] **本地化配置**: 在项目根目录生成 `.localization.config.json` 持久化分组与忽略配置。
- **作用域校验 (Scoped Validation)**: Key 唯一性检查限制在同一分组内，不同分组可重复。

### 10. 查找面板优化 (Find Panel 2.0)
- [x] **紧凑设计**: 采用输入框内嵌选项的现代化设计，节省水平空间。
- [x] **区分大小写**: 新增 `Aa` 选项，支持区分大小写的搜索与替换。
- [x] **快捷操作**: 优化的 Tab 切换与键盘交互体验。

### 11. 搜索面板交互升级 (Search UX Overhaul)
- [x] **即时搜索**: 输入内容自动触发搜索，无需回车，响应迅速。
- [x] **结果分组**: 搜索结果按文件归类折叠，结构更清晰。
- [x] **替换预览**: 实时展示 Diff 效果（删除线/高亮），操作前即可预览变更。
- [x] **智能截断**: 仅展示匹配项前后的上下文片段，避免长文本干扰，聚焦搜索结果。
- [x] **快捷操作**: 悬停显示单行替换/忽略按钮；新增输入框旁的“全部替换”快捷入口。

### 12. 搜索性能架构升级 (Search Performance 3.0)
- [x] **流式搜索**: 主进程按块返回结果，渲染端边收边展示，降低首屏等待时间。
- [x] **虚拟列表**: 查找结果面板改为 `react-window` 虚拟渲染，显著降低大结果集 DOM 压力。
- [x] **逻辑不限流**: 默认取消固定结果上限，支持全量逻辑检索。
- [x] **分页缓存**: 虚拟行使用 LRU 分页缓存，滚动按页命中，避免重复切片开销。
- [x] **脏文件优先**: 对未保存文件优先使用内存数据搜索，避免出现“搜索结果滞后于编辑内容”。

### 13. 编辑一致性修复 (Editing Consistency)
- [x] **内联编辑撤销修复**: 退出内联编辑后支持正确撤销到编辑前内容（改为通过 `updateCell` 记录历史栈）。

### 14. 应用更新能力 (App Updater)
- [x] **启动自动检查**: 应用启动后自动检查新版本（打包环境）。
- [x] **更新弹窗**: 检测到新版本后弹窗提示，展示当前版本/最新版本和更新内容。
- [x] **一键更新安装**: 点击“更新并安装”后从 GitHub 下载，完成后自动安装。
- [x] **忽略版本**: 支持忽略当前最新版本，并持久化忽略状态。
- [x] **菜单手动检查**: 顶部菜单 `Help -> Check for Updates...` 可随时打开并手动检查。

### 15. 配置跨机兼容修复 (Config Portability)
- [x] **稳定文件 ID**: 文件 ID 从“绝对路径”改为“项目相对路径”，避免跨电脑路径差异导致配置失效。
- [x] **旧配置自动迁移**: 打开项目时自动将历史配置中的旧 ID 映射到新 ID。
- [x] **跨平台一致性**: 统一路径分隔符处理，兼容 Windows/macOS。

---
> 💡 这是一个持续更新的文档。你可以在任何时候通过 `npm run dev` 运行应用并在此验证上述已勾选的功能。

### 16. CSV 兼容性修复 (2026-02-13)
- [x] **换行兼容**: 主进程读取 CSV 时增加解析回退，兼容 `\r\n`、`\n`、`\r` 行结束符，避免外部来源换行风格差异导致解析异常。
- [x] **单元测试**: 新增 `tests/unit/file-utils-newline.test.ts`，覆盖 `LF` 与 `CR` 两类行结束符场景。

### 17. 搜索面板体验修复 (2026-03-12)
- [x] **结果定位信息**: 查找结果在每条命中前展示更醒目的行号与命中列，且行号与编辑器左侧行号栏保持一致，便于快速定位。
- [x] **滚动位置保持**: 编辑文本导致搜索结果动态刷新时，虚拟列表会尽量保持当前浏览位置，不再直接跳回顶部。
- [x] **跳转自动收口编辑态**: 点击搜索结果跳转到其他位置时，会自动提交并退出当前单元格/富文本编辑态，避免旧编辑器残留焦点。
- [x] **编辑态即时预览**: 当前单元格在编辑中尚未失焦时，搜索结果也会按临时输入内容即时刷新。
- [x] **保存去抖刷新**: 仅保存文件时不再触发无意义的整轮重搜，避免查看位置丢失。

### 18. 编辑态保存修复 (2026-03-12)
- [x] **保存前同步编辑缓冲**: 手动保存时会先把当前富文本编辑的临时输入同步到项目数据；表格单元格与表头编辑由各自提交事件写入项目数据。
- [x] **编辑中未保存标记**: 文件列表会把当前编辑中尚未失焦的修改也视为未保存状态，避免出现“已经改了但文件还没变脏”的错觉。

### 19. 文件格式保真与展示 (2026-03-12)
- [x] **格式分析展示**: 读取 CSV 时分析编码、BOM 与换行风格，并在编辑区展示当前打开文件的文本格式。
- [x] **保存格式保真**: 保存时按打开文件时分析出的编码、BOM 与换行风格写回，避免把 `UTF-8 with BOM`、`LF` 等文本格式无意改写。

### 20. Excel 粘贴增强 (2026-03-12)
- [x] **多格按单元格粘贴**: 从 Excel / TSV 复制多行多列内容后，可按当前选中起点直接写入表格对应单元格。
- [x] **复杂单元格解析**: 对带引号、Tab、单双引号转义以及单元格内换行的剪贴板文本按表格语义解析，避免粘贴错列或错行。

### 21. 富文本颜色选择器体验优化 (2026-03-12)
- [x] **默认 HEX 模式**: 富文本工具栏与右键菜单的颜色选择器默认以 `HEX` 方式打开，便于直接输入常用颜色值。
- [x] **模式记忆**: 如果用户切换到 `RGB` 或 `HSL`，后续再次打开颜色选择器时会沿用上次使用的模式。

### 22. 富文本编辑即时预览修复 (2026-03-12)
- [x] **表格预览即时同步**: 富文本编辑框持续聚焦输入时，表格中当前编辑单元格的虚拟列表预览也会立即刷新，不再要等失焦或其他交互才更新。

### 23. CSV 混合换行兼容修复 (2026-03-18)
- [x] **混合换行容错**: 主进程读取 CSV 时会仅在引号外标准化记录分隔符，兼容 `CRLF`、`LF`、`CR` 混用，避免 PapaParse 因首个换行探测结果固定后出现静默串行。
- [x] **引号内换行保真**: 带引号字段中的真实多行文本不会被换行标准化逻辑改写，避免内容值被无意篡改。
- [x] **单元测试补强**: `tests/unit/file-utils-newline.test.ts` 新增混合 `CRLF/LF` 与“引号内换行 + 引号外混合换行”场景覆盖。

### 24. 左侧合并面板与最小化 (2026-05-22)
- [x] **左右面板合并**: 文件列表与查找/校验功能统一收纳到左侧工作区，编辑区占据剩余空间。
- [x] **常驻图标栏**: 最左侧始终保留文件、查找、校验图标入口，展开时右侧只显示当前选中的单一面板。
- [x] **面板最小化**: 支持通过图标栏底部按钮、`Ctrl+B` / `Cmd+B` 或再次点击已选中图标，在“仅图标栏”和“图标栏 + 当前面板”之间切换。
- [x] **查找/校验独立入口**: 查找、校验由左侧图标直接切换，不再依赖面板顶部 Tab。

### 25. 主表格渲染升级 (2026-05-22)
- [x] **Glide Data Grid 迁移**: 主 CSV 编辑表格从自研 `react-window` 网格迁移到 Glide Data Grid，优先使用原生编辑、选择、复制粘贴、删除、填充柄、尾行新增和表头菜单能力。
- [x] **适配层测试**: 新增 Glide 表格适配层单元测试，覆盖列映射、单元格内容、选区同步、批量编辑、清空删除与数字递增填充。

### 26. 外部刷新编辑态修复 (2026-06-23)
- [x] **刷新前退出编辑态**: clean 文件检测到外部变更并自动同步前，会先取消当前单元格/富文本编辑态，避免旧编辑缓冲按刷新后的行号写入错误行。
- [x] **冲突处理保持原样**: 已有本地脏数据时仍保留冲突弹窗流程，不主动提交或丢弃用户本地修改。
